<div class="goal-box">
    <a href="/"><img src="/images/Logo_Todo.png" alt="Logo" class=logo></a>
    <h1>Edit Goal</h1>
    <%- include('../components/messages.ejs') %>
    <form action="/goals/<%= goal._id %>" method="POST">
        <input type="hidden" name="_method" value="PUT">
        <input 
        type="text" 
        name="goalName"
        placeholder="Goal Name"
        value="<%= goal.goalName %>"
        >

        <p>Start Date:</p>
        <input 
        type="text"
        name="startDate"
        value="<%= helper.formatDate(goal.startDate,'MM/DD/YYYY') %>"
        >

        <p>End Date:</p>
        <input 
        type="text"
        name="endDate"
        value="<%= helper.formatDate(goal.endDate,'MM/DD/YYYY') %>">

        <p> Description:</p>
        <textarea rows="4" 
        cols="40" 
        name="description"
        placeholder="Write description if any" 
        value="<%= goal.description %>"
        ></textarea>
    
        <button type="submit">Edit Goal</button>
    
    </form>
</div>